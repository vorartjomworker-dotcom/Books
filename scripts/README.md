# Автоматические проверки

Планируемые проверки:

- `validate_blocks.py` — полнота, порядок и уникальность идентификаторов;
- `validate_terms.py` — соблюдение глоссария;
- `validate_structure.py` — структура, числа, формулы, код и ссылки.

Скрипты должны работать без изменения канонических файлов.
